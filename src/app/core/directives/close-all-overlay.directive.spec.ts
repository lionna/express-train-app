import { CloseAllOverlayDirective } from './close-all-overlay.directive';

describe('CloseAllOverlayDirective', () => {
    it('should create an instance', () => {
        const directive = new CloseAllOverlayDirective();
        expect(directive).toBeTruthy();
    });
});
